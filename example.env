# =====================================================
# CULS Environment Configuration
# =====================================================
# Copy this file to .env and update values for your environment
# IMPORTANT: Never commit .env to version control!
# =====================================================

# Application Configuration
SECRET_KEY=your-secret-key-here-change-in-production
FLASK_ENV=development
FLASK_DEBUG=1

# Database Configuration (Docker)
DATABASE_URL=postgresql://culs_user:development_password@postgres:5432/culs_db
DB_PASSWORD=development_password

# Database Configuration (Local - outside Docker)
# Uncomment and use this when running Flask locally without Docker
# DATABASE_URL=postgresql://culs_user:development_password@localhost:5432/culs_db

# Test Database (for running tests)
TEST_DATABASE_URL=postgresql://culs_user:development_password@localhost:5432/culs_test_db

# =====================================================
# Production Settings (AWS/Production Environment)
# =====================================================
# Uncomment and configure for production deployment
# FLASK_ENV=production
# FLASK_DEBUG=0
# SECRET_KEY=<generate-strong-secret-key>
# DATABASE_URL=postgresql://username:password@rds-endpoint:5432/culs_db
# DB_PASSWORD=<strong-password>

# AWS Configuration (for future use)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=us-east-1

# Email Configuration (for future notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=
# MAIL_PASSWORD=

# =====================================================
# Notes
# =====================================================
# 1. Generate SECRET_KEY with: python -c "import secrets; print(secrets.token_hex(32))"
# 2. For Docker: Use service name 'postgres' as database host
# 3. For local: Use 'localhost' as database host
# 4. Default credentials are for DEVELOPMENT ONLY
# 5. Always use strong passwords in production
